---
import Layout from '../../layouts/Layout.astro';
import ReactMap from '../../components/Reactmap';

const { team } = Astro.params;
---

<Layout title={team}>
  <ReactMap client:only='react' team={team} />
</Layout>
